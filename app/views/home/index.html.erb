<h1>Galpões & Estoque</h1>

<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <%= w.name %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  Área: <%= number_with_delimiter(w.area, delimiter: '.') %> m²
<% end %>

<% if @warehouses.empty? %>
  <p>Não existem galpões cadastrados.</p>
<% end %>